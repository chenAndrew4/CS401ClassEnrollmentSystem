package shared.models.responses;

import shared.enums.MessageStatus;
import shared.enums.MessageType;

public class AddScheduleSectionResponse {
    public AddScheduleSectionResponse(MessageStatus messageStatus, MessageType messageType, String added) {
    }
}
